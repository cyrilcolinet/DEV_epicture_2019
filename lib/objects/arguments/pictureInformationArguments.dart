// You can pass any object to the arguments parameter.
// In this example, create a class that contains a customizable
// title and message.
class PictureInformationArguments {
    final dynamic image;

    /// Constructor
    PictureInformationArguments(this.image);
}